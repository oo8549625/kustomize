apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: istio-sample-gateway
spec:
  selector:
    istio: ingress # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "default/istio-sample.test.miyo"
---