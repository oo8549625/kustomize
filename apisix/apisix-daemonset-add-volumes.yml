apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  labels:
    app.iyes.dev/name: gateway
spec:
  template:
    spec:
      containers:
        - name: apisix
          volumeMounts:
            - name: ngx-tpl-config
              mountPath: /usr/local/apisix/apisix/cli/ngx_tpl.lua
              subPath: ngx_tpl.lua
      volumes:
        - name: ngx-tpl-config
          configMap:
            name: ngx-tpl