apiVersion: v1
kind: Service
metadata:
  name: apisix
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: apisix
  externalTrafficPolicy: Local
  ports:
    - protocol: TCP
      port: 9080
      targetPort: 9080
      nodePort: 32350
