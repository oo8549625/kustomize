apiVersion: v1
kind: Service
metadata:
  name: apisix
spec:
  type: NodePort
  selector:
    app: apisix
  externalTrafficPolicy: Cluster
  ports:
    - protocol: TCP
      port: 9080
      targetPort: 9080
      nodePort: 32350
